/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/axios'
import type { HeroStatsQueryResponse, HeroStatsQueryParams, HeroStats400, HeroStats500 } from '../types/HeroStats.ts'
import type { RequestConfig, ResponseErrorConfig } from '@kubb/plugin-client/clients/axios'

function getHeroStatsUrl() {
  const res = { method: 'GET', url: `/v1/analytics/hero-stats` as const }
  return res
}

/**
 * @description Retrieves performance statistics for each hero based on historical match data.### Rate Limits:| Type | Limit || ---- | ----- || IP | 100req/s || Key | - || Global | - |
 * @summary Hero Stats
 * {@link /v1/analytics/hero-stats}
 */
export async function heroStats(params?: HeroStatsQueryParams, config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<HeroStatsQueryResponse, ResponseErrorConfig<HeroStats400 | HeroStats500>, unknown>({
    method: 'GET',
    url: getHeroStatsUrl().url.toString(),
    params,
    ...requestConfig,
  })
  return res.data
}

function getHeroStatsUrl() {
  const res = { method: 'GET', url: `/v1/players/hero-stats` as const }
  return res
}

/**
 * @description This endpoint returns statistics for each hero played by a given player account.### Rate Limits:| Type | Limit || ---- | ----- || IP | 100req/s || Key | - || Global | - |
 * @summary Hero Stats
 * {@link /v1/players/hero-stats}
 */
export async function heroStats(params: HeroStatsQueryParams, config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<HeroStatsQueryResponse, ResponseErrorConfig<HeroStats400 | HeroStats500>, unknown>({
    method: 'GET',
    url: getHeroStatsUrl().url.toString(),
    params,
    ...requestConfig,
  })
  return res.data
}