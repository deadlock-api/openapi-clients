/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/axios'
import type { HealthCheckQueryResponse, HealthCheck500 } from '../types/HealthCheck.ts'
import type { RequestConfig, ResponseErrorConfig } from '@kubb/plugin-client/clients/axios'

function getHealthCheckUrl() {
  const res = { method: 'GET', url: `/v1/info/health` as const }
  return res
}

/**
 * @description Checks the health of the services.### Rate Limits:| Type | Limit || ---- | ----- || IP | 100req/s || Key | - || Global | - |
 * @summary Health Check
 * {@link /v1/info/health}
 */
export async function healthCheck(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<HealthCheckQueryResponse, ResponseErrorConfig<HealthCheck500>, unknown>({
    method: 'GET',
    url: getHealthCheckUrl().url.toString(),
    ...requestConfig,
  })
  return res.data
}