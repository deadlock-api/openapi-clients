/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@kubb/plugin-client/clients/axios";
import type { ActiveMatchesQueryResponse, ActiveMatchesQueryParams, ActiveMatches400, ActiveMatches500 } from "../types/ActiveMatches.ts";
import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getActiveMatchesUrl() {
  const res = { method: 'GET', url: `/v1/matches/active` as const }
return res
}

/**
 * @description Returns active matches that are currently being played.Fetched from the watch tab in game, which is limited to the **top 200 matches**.### Rate Limits:| Type | Limit || ---- | ----- || IP | 100req/s || Key | - || Global | - |
 * @summary Active
 * {@link /v1/matches/active}
 */
export async function activeMatches(params?: ActiveMatchesQueryParams, config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client:request = fetch, ...requestConfig } = config


const res = await request<ActiveMatchesQueryResponse, ResponseErrorConfig<ActiveMatches400 | ActiveMatches500>, unknown>({ method : "GET", url : getActiveMatchesUrl().url.toString(), params, ... requestConfig })
return res.data
}