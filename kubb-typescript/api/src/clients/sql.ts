/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/axios'
import type { SqlQueryResponse, SqlQueryParams, Sql500 } from '../types/Sql.ts'
import type { RequestConfig, ResponseErrorConfig } from '@kubb/plugin-client/clients/axios'

function getSqlUrl() {
  const res = { method: 'GET', url: `/v1/sql` as const }
  return res
}

/**
 * @description Executes a SQL query on the database.### Rate Limits:| Type | Limit || ---- | ----- || IP | 300req/5min || Key | 300req/5min || Global | 600req/60s |
 * @summary Query
 * {@link /v1/sql}
 */
export async function sql(params: SqlQueryParams, config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<SqlQueryResponse, ResponseErrorConfig<Sql500>, unknown>({
    method: 'GET',
    url: getSqlUrl().url.toString(),
    params,
    ...requestConfig,
  })
  return res.data
}