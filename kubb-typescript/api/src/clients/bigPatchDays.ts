/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@kubb/plugin-client/clients/axios";
import type { BigPatchDaysQueryResponse } from "../types/BigPatchDays.ts";
import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";

function getBigPatchDaysUrl() {
  const res = { method: 'GET', url: `/v1/patches/big-days` as const }
return res
}

/**
 * @description Returns a list of dates where Deadlock's "big" patch days were, usually bi-weekly.The exact date is the time when the announcement forum post was published.This list is manually maintained, and so new patch dates may be delayed by a few hours.### Rate Limits:| Type | Limit || ---- | ----- || IP | 100req/s || Key | - || Global | - |
 * @summary Big Days
 * {@link /v1/patches/big-days}
 */
export async function bigPatchDays(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client:request = fetch, ...requestConfig } = config


const res = await request<BigPatchDaysQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getBigPatchDaysUrl().url.toString(), ... requestConfig })
return res.data
}