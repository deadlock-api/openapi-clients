/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package assets_deadlock_api_client.models


import com.google.gson.Gson
import com.google.gson.JsonElement
import com.google.gson.TypeAdapter
import com.google.gson.TypeAdapterFactory
import com.google.gson.reflect.TypeToken
import com.google.gson.stream.JsonReader
import com.google.gson.stream.JsonWriter
import com.google.gson.annotations.JsonAdapter
import java.io.IOException
import com.google.gson.annotations.SerializedName
import java.io.Serializable

/**
 * 
 *
 * @param maxPurchasesForTier 
 */


data class RawHeroItemSlotInfoValueV2 (

    @SerializedName("max_purchases_for_tier")
    val maxPurchasesForTier: kotlin.collections.List<kotlin.Int>

) : Serializable {
    companion object {
        private const val serialVersionUID: Long = 123
    }


    class CustomTypeAdapterFactory : TypeAdapterFactory {
        override fun <T> create(gson: Gson, type: TypeToken<T>): TypeAdapter<T>? {
            if (!RawHeroItemSlotInfoValueV2::class.java.isAssignableFrom(type.rawType)) {
              return null // this class only serializes 'RawHeroItemSlotInfoValueV2' and its subtypes
            }
            val elementAdapter = gson.getAdapter(JsonElement::class.java)
            val thisAdapter = gson.getDelegateAdapter(this, TypeToken.get(RawHeroItemSlotInfoValueV2::class.java))

            @Suppress("UNCHECKED_CAST")
            return object : TypeAdapter<RawHeroItemSlotInfoValueV2>() {
                @Throws(IOException::class)
                override fun write(out: JsonWriter, value: RawHeroItemSlotInfoValueV2) {
                    val obj = thisAdapter.toJsonTree(value).getAsJsonObject()
                    elementAdapter.write(out, obj)
                }

                @Throws(IOException::class)
                override fun read(jsonReader: JsonReader): RawHeroItemSlotInfoValueV2  {
                    val jsonElement = elementAdapter.read(jsonReader)
                    validateJsonElement(jsonElement)
                    return thisAdapter.fromJsonTree(jsonElement)
                }
            }.nullSafe() as TypeAdapter<T>
        }
    }

    companion object {
        var openapiFields = HashSet<String>()
        var openapiRequiredFields = HashSet<String>()

        init {
            // a set of all properties/fields (JSON key names)
            openapiFields.add("max_purchases_for_tier")

            // a set of required properties/fields (JSON key names)
            openapiRequiredFields.add("max_purchases_for_tier")
        }

       /**
        * Validates the JSON Element and throws an exception if issues found
        *
        * @param jsonElement JSON Element
        * @throws IOException if the JSON Element is invalid with respect to RawHeroItemSlotInfoValueV2
        */
        @Throws(IOException::class)
        fun validateJsonElement(jsonElement: JsonElement?) {
            if (jsonElement == null) {
              require(openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                String.format("The required field(s) %s in RawHeroItemSlotInfoValueV2 is not found in the empty JSON string", RawHeroItemSlotInfoValueV2.openapiRequiredFields.toString())
              }
            }

            // check to make sure all required properties/fields are present in the JSON string
            for (requiredField in openapiRequiredFields) {
              requireNotNull(jsonElement!!.getAsJsonObject()[requiredField]) {
                String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString())
              }
            }
            val jsonObj = jsonElement!!.getAsJsonObject()
            // ensure the required json array is present
            requireNotNull(jsonObj["max_purchases_for_tier"]) {
              "Expected the field `max_purchases_for_tier` to be an array in the JSON string but got `null`"
            }
            require(jsonObj["max_purchases_for_tier"].isJsonArray()) {
              String.format("Expected the field `max_purchases_for_tier` to be an array in the JSON string but got `%s`", jsonObj["max_purchases_for_tier"].toString())
            }
            // ensure the items in json array are primitive
            if (jsonObj["max_purchases_for_tier"] != null) {
              for (i in 0 until jsonObj.getAsJsonArray("max_purchases_for_tier").size()) {
                require(jsonObj.getAsJsonArray("max_purchases_for_tier").get(i).isJsonPrimitive) {
                  String.format("Expected the property in array `max_purchases_for_tier` to be primitive")
                }
              }
            }
        }
    }

}

