/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package deadlock_api_client.models

import deadlock_api_client.models.BuildHeroDetailsAbilityOrderCurrencyChange

import com.google.gson.Gson
import com.google.gson.JsonElement
import com.google.gson.TypeAdapter
import com.google.gson.TypeAdapterFactory
import com.google.gson.reflect.TypeToken
import com.google.gson.stream.JsonReader
import com.google.gson.stream.JsonWriter
import com.google.gson.annotations.JsonAdapter
import java.io.IOException
import com.google.gson.annotations.SerializedName
import java.io.Serializable

/**
 * 
 *
 * @param currencyChanges 
 */


data class BuildHeroDetailsAbilityOrder (

    @SerializedName("currency_changes")
    val currencyChanges: kotlin.collections.List<BuildHeroDetailsAbilityOrderCurrencyChange>? = null

) : Serializable {
    companion object {
        private const val serialVersionUID: Long = 123
    }


    class CustomTypeAdapterFactory : TypeAdapterFactory {
        override fun <T> create(gson: Gson, type: TypeToken<T>): TypeAdapter<T>? {
            if (!BuildHeroDetailsAbilityOrder::class.java.isAssignableFrom(type.rawType)) {
              return null // this class only serializes 'BuildHeroDetailsAbilityOrder' and its subtypes
            }
            val elementAdapter = gson.getAdapter(JsonElement::class.java)
            val thisAdapter = gson.getDelegateAdapter(this, TypeToken.get(BuildHeroDetailsAbilityOrder::class.java))

            @Suppress("UNCHECKED_CAST")
            return object : TypeAdapter<BuildHeroDetailsAbilityOrder>() {
                @Throws(IOException::class)
                override fun write(out: JsonWriter, value: BuildHeroDetailsAbilityOrder) {
                    val obj = thisAdapter.toJsonTree(value).getAsJsonObject()
                    elementAdapter.write(out, obj)
                }

                @Throws(IOException::class)
                override fun read(jsonReader: JsonReader): BuildHeroDetailsAbilityOrder  {
                    val jsonElement = elementAdapter.read(jsonReader)
                    validateJsonElement(jsonElement)
                    return thisAdapter.fromJsonTree(jsonElement)
                }
            }.nullSafe() as TypeAdapter<T>
        }
    }

    companion object {
        var openapiFields = HashSet<String>()
        var openapiRequiredFields = HashSet<String>()

        init {
            // a set of all properties/fields (JSON key names)
            openapiFields.add("currency_changes")

        }

       /**
        * Validates the JSON Element and throws an exception if issues found
        *
        * @param jsonElement JSON Element
        * @throws IOException if the JSON Element is invalid with respect to BuildHeroDetailsAbilityOrder
        */
        @Throws(IOException::class)
        fun validateJsonElement(jsonElement: JsonElement?) {
            if (jsonElement == null) {
              require(openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                String.format("The required field(s) %s in BuildHeroDetailsAbilityOrder is not found in the empty JSON string", BuildHeroDetailsAbilityOrder.openapiRequiredFields.toString())
              }
            }
            val jsonObj = jsonElement!!.getAsJsonObject()
            if (jsonObj["currency_changes"] != null && !jsonObj["currency_changes"].isJsonNull) {
              if (jsonObj.getAsJsonArray("currency_changes") != null) {
                // ensure the json data is an array
                require(jsonObj["currency_changes"].isJsonArray) {
                  String.format("Expected the field `currency_changes` to be an array in the JSON string but got `%s`", jsonObj["currency_changes"].toString())
                }

                // validate the optional field `currency_changes` (array)
                for (i in 0 until jsonObj.getAsJsonArray("currency_changes").size()) {
                  BuildHeroDetailsAbilityOrderCurrencyChange.validateJsonElement(jsonObj.getAsJsonArray("currency_changes").get(i))
                }
              }
            }
        }
    }

}

