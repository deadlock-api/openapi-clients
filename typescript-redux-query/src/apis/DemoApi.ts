// tslint:disable
/**
 * Deadlock API
 *  ## API Clients  We have auto generated and updated clients for many languages. You can find them here: [https://github.com/deadlock-api/openapi-clients](https://github.com/deadlock-api/openapi-clients)  ## Support the Deadlock API  Whether you\'re building your own database, developing data science projects, or enhancing your website with game and player analytics, the Deadlock API has the data you need.  Your sponsorship helps keep this resource open, free and future-proof for everyone. By supporting the Deadlock API, you will enable continued development, new features and reliable access for developers, analysts and streamers worldwide.  Help us continue to provide the data you need - sponsor the Deadlock API today!  **-> You can Sponsor the Deadlock API on [Patreon](https://www.patreon.com/c/user?u=68961896) or [GitHub](https://github.com/sponsors/raimannma)**  ## Disclaimer _deadlock-api.com is not endorsed by Valve and does not reflect the views or opinions of Valve or anyone officially involved in producing or managing Valve properties. Valve and all associated properties are trademarks or registered trademarks of Valve Corporation_         
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    DemoQueryJobResponse,
    DemoQueryJobResponseFromJSON,
    DemoQueryJobResponseToJSON,
    DemoQueryRequest,
    DemoQueryRequestFromJSON,
    DemoQueryRequestToJSON,
    DemoQueryStatusResponse,
    DemoQueryStatusResponseFromJSON,
    DemoQueryStatusResponseToJSON,
    DemoSchemaResponse,
    DemoSchemaResponseFromJSON,
    DemoSchemaResponseToJSON,
} from '../models';

export interface SchemaRequest {
    matchId?: number;
}

export interface StatusRequest {
    jobId: string;
}

export interface SubmitRequest {
    demoQueryRequest: DemoQueryRequest;
}


/**
 *  Returns the queryable schema of a match\'s demo file: every entity and event table with its columns and Arrow types.  By default this returns the schema of the most recent match we have a demo for. Optionally pass `match_id` to read the schema for a specific match; if we don\'t already have its salts, they are fetched from Steam (rate limited, see `/{match_id}/salts`).     
 * Demo Schema
 */
function schemaRaw<T>(requestParameters: SchemaRequest, requestConfig: runtime.TypedQueryConfig<T, DemoSchemaResponse> = {}): QueryConfig<T> {
    let queryParameters = null;

    queryParameters = {};


    if (requestParameters.matchId !== undefined) {
        queryParameters['match_id'] = requestParameters.matchId;
    }

    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/matches/demo/schema`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DemoSchemaResponseFromJSON(body), text);
    }

    return config;
}

/**
*  Returns the queryable schema of a match\'s demo file: every entity and event table with its columns and Arrow types.  By default this returns the schema of the most recent match we have a demo for. Optionally pass `match_id` to read the schema for a specific match; if we don\'t already have its salts, they are fetched from Steam (rate limited, see `/{match_id}/salts`).     
* Demo Schema
*/
export function schema<T>(requestParameters: SchemaRequest, requestConfig?: runtime.TypedQueryConfig<T, DemoSchemaResponse>): QueryConfig<T> {
    return schemaRaw(requestParameters, requestConfig);
}

/**
 *  Returns the status of a demo query job. While `queued`/`running` it includes a rough `estimated_wait_seconds`; when `done` it includes `result_url` (a public link to the Parquet/NDJSON artifact); when `failed` it includes `error`. 
 * Demo Query Status
 */
function statusRaw<T>(requestParameters: StatusRequest, requestConfig: runtime.TypedQueryConfig<T, DemoQueryStatusResponse> = {}): QueryConfig<T> {
    if (requestParameters.jobId === null || requestParameters.jobId === undefined) {
        throw new runtime.RequiredError('jobId','Required parameter requestParameters.jobId was null or undefined when calling status.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/matches/demo/query/{job_id}`.replace('{job_id}', encodeURIComponent(String(requestParameters.jobId))),
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DemoQueryStatusResponseFromJSON(body), text);
    }

    return config;
}

/**
*  Returns the status of a demo query job. While `queued`/`running` it includes a rough `estimated_wait_seconds`; when `done` it includes `result_url` (a public link to the Parquet/NDJSON artifact); when `failed` it includes `error`. 
* Demo Query Status
*/
export function status<T>(requestParameters: StatusRequest, requestConfig?: runtime.TypedQueryConfig<T, DemoQueryStatusResponse>): QueryConfig<T> {
    return statusRaw(requestParameters, requestConfig);
}

/**
 *  Submit a SQL query against a match\'s demo file. The work (download + decompress + parse + query) takes ~12s, so this is asynchronous: the endpoint returns a `job_id` you poll via `/demo/query/{job_id}`. Once done, the status response carries a public URL to the result artifact (Parquet or NDJSON).  Identical `(match_id, query, format)` submissions are deduplicated and reuse a cached result.  ### Rate Limits: | Type | Limit | | ---- | ----- | | IP | 20req/h | | Key | 200req/h | | Global | 400req/h | 
 * Demo Query
 */
function submitRaw<T>(requestParameters: SubmitRequest, requestConfig: runtime.TypedQueryConfig<T, DemoQueryJobResponse> = {}): QueryConfig<T> {
    if (requestParameters.demoQueryRequest === null || requestParameters.demoQueryRequest === undefined) {
        throw new runtime.RequiredError('demoQueryRequest','Required parameter requestParameters.demoQueryRequest was null or undefined when calling submit.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/matches/demo/query`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || DemoQueryRequestToJSON(requestParameters.demoQueryRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(DemoQueryJobResponseFromJSON(body), text);
    }

    return config;
}

/**
*  Submit a SQL query against a match\'s demo file. The work (download + decompress + parse + query) takes ~12s, so this is asynchronous: the endpoint returns a `job_id` you poll via `/demo/query/{job_id}`. Once done, the status response carries a public URL to the result artifact (Parquet or NDJSON).  Identical `(match_id, query, format)` submissions are deduplicated and reuse a cached result.  ### Rate Limits: | Type | Limit | | ---- | ----- | | IP | 20req/h | | Key | 200req/h | | Global | 400req/h | 
* Demo Query
*/
export function submit<T>(requestParameters: SubmitRequest, requestConfig?: runtime.TypedQueryConfig<T, DemoQueryJobResponse>): QueryConfig<T> {
    return submitRaw(requestParameters, requestConfig);
}

