// tslint:disable
/**
 * Deadlock API
 *  ## API Clients  We have auto generated and updated clients for many languages. You can find them here: [https://github.com/deadlock-api/openapi-clients](https://github.com/deadlock-api/openapi-clients)  ## Support the Deadlock API  Whether you\'re building your own database, developing data science projects, or enhancing your website with game and player analytics, the Deadlock API has the data you need.  Your sponsorship helps keep this resource open, free and future-proof for everyone. By supporting the Deadlock API, you will enable continued development, new features and reliable access for developers, analysts and streamers worldwide.  Help us continue to provide the data you need - sponsor the Deadlock API today!  **-> You can Sponsor the Deadlock API on [Patreon](https://www.patreon.com/c/user?u=68961896) or [GitHub](https://github.com/sponsors/raimannma)**  ## Disclaimer _deadlock-api.com is not endorsed by Valve and does not reflect the views or opinions of Valve or anyone officially involved in producing or managing Valve properties. Valve and all associated properties are trademarks or registered trademarks of Valve Corporation_         
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { HttpMethods, QueryConfig, ResponseBody, ResponseText } from 'redux-query';
import * as runtime from '../runtime';
import {
    ListServersResponse,
    ListServersResponseFromJSON,
    ListServersResponseToJSON,
    MetricIngestRequest,
    MetricIngestRequestFromJSON,
    MetricIngestRequestToJSON,
    ServerStatusRequest,
    ServerStatusRequestFromJSON,
    ServerStatusRequestToJSON,
    ServerStatusResponse,
    ServerStatusResponseFromJSON,
    ServerStatusResponseToJSON,
    SteamServer,
    SteamServerFromJSON,
    SteamServerToJSON,
} from '../models';

export interface IngestRequest {
    metricIngestRequest: MetricIngestRequest;
}

export interface StatusRequest {
    serverStatusRequest: ServerStatusRequest;
}


/**
 *  Ingests a single metric event reported by a game server. The schema is intentionally flexible: `metric_value` carries the primary numeric measurement and `metadata` holds arbitrary key/value context that varies per game mode or metric. Optional `map` and `game_mode_version` let callers segment leaderboards per map or per ruleset revision. Requires a valid game server secret as a Bearer token.     
 * Game Server Metric Ingest
 */
function ingestRaw<T>(requestParameters: IngestRequest, requestConfig: runtime.TypedQueryConfig<T, void> = {}): QueryConfig<T> {
    if (requestParameters.metricIngestRequest === null || requestParameters.metricIngestRequest === undefined) {
        throw new runtime.RequiredError('metricIngestRequest','Required parameter requestParameters.metricIngestRequest was null or undefined when calling ingest.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/servers/metrics`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || MetricIngestRequestToJSON(requestParameters.metricIngestRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
    }

    return config;
}

/**
*  Ingests a single metric event reported by a game server. The schema is intentionally flexible: `metric_value` carries the primary numeric measurement and `metadata` holds arbitrary key/value context that varies per game mode or metric. Optional `map` and `game_mode_version` let callers segment leaderboards per map or per ruleset revision. Requires a valid game server secret as a Bearer token.     
* Game Server Metric Ingest
*/
export function ingest<T>(requestParameters: IngestRequest, requestConfig?: runtime.TypedQueryConfig<T, void>): QueryConfig<T> {
    return ingestRaw(requestParameters, requestConfig);
}

/**
 * Returns all currently active game servers.
 * List Game Servers
 */
function listRaw<T>( requestConfig: runtime.TypedQueryConfig<T, ListServersResponse> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/servers`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ListServersResponseFromJSON(body), text);
    }

    return config;
}

/**
* Returns all currently active game servers.
* List Game Servers
*/
export function list<T>( requestConfig?: runtime.TypedQueryConfig<T, ListServersResponse>): QueryConfig<T> {
    return listRaw( requestConfig);
}

/**
 *  Reports the current status of a game server. Game servers must call this endpoint at least once every 30 seconds to remain active. Requires a valid game server secret as a Bearer token.     
 * Game Server Status
 */
function statusRaw<T>(requestParameters: StatusRequest, requestConfig: runtime.TypedQueryConfig<T, ServerStatusResponse> = {}): QueryConfig<T> {
    if (requestParameters.serverStatusRequest === null || requestParameters.serverStatusRequest === undefined) {
        throw new runtime.RequiredError('serverStatusRequest','Required parameter requestParameters.serverStatusRequest was null or undefined when calling status.');
    }

    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};

    headerParameters['Content-Type'] = 'application/json';


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/servers/status`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'POST',
            headers: headerParameters,
        },
        body: queryParameters || ServerStatusRequestToJSON(requestParameters.serverStatusRequest),
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(ServerStatusResponseFromJSON(body), text);
    }

    return config;
}

/**
*  Reports the current status of a game server. Game servers must call this endpoint at least once every 30 seconds to remain active. Requires a valid game server secret as a Bearer token.     
* Game Server Status
*/
export function status<T>(requestParameters: StatusRequest, requestConfig?: runtime.TypedQueryConfig<T, ServerStatusResponse>): QueryConfig<T> {
    return statusRaw(requestParameters, requestConfig);
}

/**
 *  Returns the list of Deadlock game servers registered with the Steam master server (`IGameServersService/GetServerList`), filtered to Deadlock\'s appid.     
 * List Steam Game Servers
 */
function steamListRaw<T>( requestConfig: runtime.TypedQueryConfig<T, Array<SteamServer>> = {}): QueryConfig<T> {
    let queryParameters = null;


    const headerParameters : runtime.HttpHeaders = {};


    const { meta = {} } = requestConfig;

    const config: QueryConfig<T> = {
        url: `${runtime.Configuration.basePath}/v1/servers/steam`,
        meta,
        update: requestConfig.update,
        queryKey: requestConfig.queryKey,
        optimisticUpdate: requestConfig.optimisticUpdate,
        force: requestConfig.force,
        rollback: requestConfig.rollback,
        options: {
            method: 'GET',
            headers: headerParameters,
        },
        body: queryParameters,
    };

    const { transform: requestTransform } = requestConfig;
    if (requestTransform) {
        config.transform = (body: ResponseBody, text: ResponseBody) => requestTransform(body.map(SteamServerFromJSON), text);
    }

    return config;
}

/**
*  Returns the list of Deadlock game servers registered with the Steam master server (`IGameServersService/GetServerList`), filtered to Deadlock\'s appid.     
* List Steam Game Servers
*/
export function steamList<T>( requestConfig?: runtime.TypedQueryConfig<T, Array<SteamServer>>): QueryConfig<T> {
    return steamListRaw( requestConfig);
}

